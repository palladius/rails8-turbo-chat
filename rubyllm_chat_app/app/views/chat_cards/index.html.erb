<div class="container mx-auto px-4">
  <h1 class="text-2xl font-bold text-center my-8">Chat Cards</h1>
  <div class="text-center mb-8">
    <%= link_to "Go to Chats", chats_path, class: "bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded" %>
  </div>
  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
    <% @chats.each do |chat| %>
      <div class="max-w-sm rounded overflow-hidden shadow-lg">
        <% if chat.generated_image.attached? %>
          <%= image_tag chat.generated_image, class: "w-full" %>
        <% end %>
        <div class="px-6 py-4">
          <div class="font-bold text-xl mb-2"><%= link_to chat.title, chat %></div>
        </div>
      </div>
    <% end %>
  </div>
</div>
