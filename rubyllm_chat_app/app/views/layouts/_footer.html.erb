<footer class="bg-slate-900/50 backdrop-blur-md border-t border-white/10 text-gray-400 text-xs text-center p-4 mt-8">
  <div class="w-full px-4 sm:px-6 lg:px-8">
    <%= APP_NAME %> v<span class="font-bold text-white"><%= APP_VERSION %></span>
    | 🐙 <%= link_to "GitHub", "https://github.com/palladius/rails8-turbo-chat", class: "text-blue-400 hover:underline" %>
     <%= link_to '#' + GIT_COMMIT_HASH, "https://github.com/palladius/rails8-turbo-chat/commit/#{GIT_COMMIT_HASH}", class: "text-blue-400 hover:underline font-mono" %>
    | 🐛 <%= link_to "File a bug", "https://github.com/palladius/rails8-turbo-chat/issues/new", class: "text-blue-400 hover:underline", target: "_blank", rel: "noopener" %>
    <% if defined?(CLOUD_RUN_ENDPOINTS) && CLOUD_RUN_ENDPOINTS[:dev].present? %>
      | 🚀 <%= link_to "Dev", CLOUD_RUN_ENDPOINTS[:dev], class: "text-blue-400 hover:underline" %>
    <% end %>
    <% if defined?(CLOUD_RUN_ENDPOINTS) && CLOUD_RUN_ENDPOINTS[:prod].present? %>
      | 🚀 <%= link_to "Prod", CLOUD_RUN_ENDPOINTS[:prod], class: "text-blue-400 hover:underline" %>
    <% end %>
    | Made with <%= raw '💛' %> by <%= link_to "Riccardo", "https://www.linkedin.com/in/riccardocarlesso/", class: "text-blue-400 hover:underline" %>

    <%# Environment Indicator %>
    <% if Rails.env.development? %>
      <span class="text-yellow-500 font-bold ml-2">DEV</span>
    <% elsif Rails.env.production? %>
      <span class="text-green-500 font-bold ml-2">PROD</span>
    <% elsif Rails.env.test? %>
      <span class="text-red-500 font-bold ml-2">TEST</span>
    <% end %>
  </div>
</footer>

<% if Rails.env.development? && request.local? && ENV["WEB_CONSOLE"] == "1" %>
  <%= console %>
<% end %>
