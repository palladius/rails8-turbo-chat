#!/bin/bash
# Script to run the Rails application in Docker with live file synchronization

set -e

echo "Starting Rails application with Docker Compose..."
echo "The application will be available at http://localhost:3000"
echo "Press Ctrl+C to stop the services"
echo ""

# Start the services
docker-compose up

echo ""
echo "Services stopped. To start again, run: bin/docker-dev"
